$('.flash').html('<%= j render partial: 'shared/flash' %>');
$('.cancel-vote-button').html('<%=
 if policy(Question).cancel_vote?
  link_to 'Cancel vote', cancel_vote_questions_path, class: 'btn btn-secondary my-2', method: :delete, remote: true
 end %>');
$('.questions').html('<%= j render partial: 'questions/question', collection: @questions %>')