h1 = @question.title
span = @question.body

.row
  .col
    .answer_it
      = link_to 'Answer it', new_question_answer_path(@question), class: 'btn btn-primary', remote: true
.row
  .col
    = link_to 'Back to Questions', questions_path, class: 'btn btn-secondary my-2'

.row
  .col-12
    h3 Answers
    == pagy_bootstrap_nav @pagy
    .answers
      = render @answers.order_best
    == pagy_bootstrap_nav @pagy